println("example => ${0 + 0}")

0 until 1.shl(5)

val count = 3
0b101.shr(count - 1) and 1
0b101.shr(count - 2) and 1
0b101.shr(count - 3) and 1

val firstValue = 0b101.shr(count - 1) and 1
val secondValue = 0b101.shr(count - 2) and 1

"0XX0"
    .replaceFirst('X', (firstValue + 48).toChar())
    .replaceFirst('X', (secondValue + 48).toChar())